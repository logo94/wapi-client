# wapi-client
JavaScript client for Wikidata and custom Wikibase instances. It uses WAPI extension to safely handle CORS and authentication, enabling direct, secure read/write access to the Wiki API from any isolated browser context.
